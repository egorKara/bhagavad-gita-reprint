#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Проверяем только изменённые файлы
npx lint-staged

# Дополнительные проверки
echo "🔍 Запуск быстрых проверок безопасности..."
npm audit --audit-level high --production

echo "✅ Pre-commit проверки завершены успешно!"